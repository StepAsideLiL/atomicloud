import React from "react";

const Logo = () => {
  return (
    <div className="text-3xl font-black select-none">
      A
      <span className="text-xs font-semibold">
        t<span className="text-3xl font-black">.</span>mi
      </span>
      C
      <span className="text-xs font-semibold">
        l<span className="text-3xl font-black">.</span>ud
      </span>
    </div>
  );
};

export default Logo;
